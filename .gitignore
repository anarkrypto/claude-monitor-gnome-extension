# Claude Code local, per-machine settings — not project configuration.
.claude/settings.local.json

# Brainstorming and planning scratch. Useful while the work is in flight, but
# the design that survives belongs in docs/ and in commit messages, not here.
docs/superpowers/

# Editors / OS
*.swp
*~
.DS_Store
